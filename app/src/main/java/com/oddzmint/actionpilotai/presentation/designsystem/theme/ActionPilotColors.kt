package com.oddzmint.actionpilotai.presentation.designsystem.theme

import android.view.Surface
import androidx.compose.ui.graphics.Color
import androidx.core.content.pm.ShortcutInfoCompat

object ActionPilotColors {
    val Background = Color(0xFF121212)
    val Surface = Color(0xFF1E1E1E)
    val Primary = Color(0xFF4F8CFF)
    val Secondary = Color(0xFF03DAC5)
    val Success = Color(0xFF4CAF50)
    val Error = Color(0xFFFF5252)
    val UserMessageBubble = Color(0xFF4F8CFF)
    val AiMessageBubble = Color(0xFF2A2A2A)
    val VoiceListening = Color(0xFFFF6B6B)
}